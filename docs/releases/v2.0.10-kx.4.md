<!--LANG:en-->
# Cherry Studio 2.0.10-kx.4

This Kon-x stable fork release tracks official Cherry Studio 2.0.10.

## Fixed

### Network Stability

- **AI requests now automatically retry on transient network errors.** When a request fails with `net::ERR_FAILED`, `net::ERR_NETWORK_CHANGED`, proxy connection failure, DNS resolution failure, connection reset, or connection refused, it retries once after a 1-second delay without requiring app restart or proxy setting toggle. The retry applies only to the failed request without disrupting other ongoing conversation streams.

  This addresses connectivity loss after network path changes — VPN or game accelerator startup, network adapter changes, or routing table updates — that the interface-fingerprint monitoring in kx.3 cannot detect (when the accelerator manipulates routing tables without adding or removing network interfaces).

## Fork baseline

- Tracks official stable upstream tags while preserving upstream Git history.
- Keeps the API Gateway and channel bot adapters removed.
- Supports direct-provider Claude Code and DeepSeek Harness connections; gateway-only routes are rejected.
- Preserves deletable preset providers, batch provider deletion, and web-grounded selection explanations.
- PDF (BabelDOC) translation talks to the selected provider's own OpenAI-compatible endpoint instead of the removed gateway.

Setup builds update only from `Kon-x/cherry-studio` Latest Releases. The portable build remains manually updated.
Windows binaries are not Authenticode-signed and may show a SmartScreen warning.

<!--LANG:zh-CN-->
# Cherry Studio 2.0.10-kx.4

本 Kon-x 稳定定制版跟随 Cherry Studio 官方 2.0.10。

## 修复

### 网络稳定性

- **AI 请求现在会在瞬态网络错误时自动重试。** 当请求因 `net::ERR_FAILED`、`net::ERR_NETWORK_CHANGED`、代理连接失败、DNS 解析失败、连接重置或连接拒绝而失败时，会在 1 秒延迟后自动重试一次，无需重启应用或切换代理设置。重试仅针对失败的单个请求，不会中断其他正在进行的对话流。

  这解决了网络路径变化后的连接丢失问题——VPN 或游戏加速器启动、网络适配器变化、或路由表更新——kx.3 的网卡指纹监控无法检测到这些情况（当加速器仅操作路由表而不增删网卡时）。

## 定制版基线

- 仅跟随上游正式稳定标签，并保留完整上游 Git 历史。
- 继续移除 API Gateway 和频道机器人适配器。
- Claude Code 与 DeepSeek Harness 仅支持直连服务商；必须经过网关的路由会被明确拒绝。
- 保留预置服务商删除、批量删除服务商和划词解释强制联网能力。
- PDF（BabelDOC）翻译改为直连所选服务商自身的 OpenAI 兼容端点，不再依赖已移除的网关。

安装版仅从 `Kon-x/cherry-studio` 的 Latest Release 更新；便携版仍需手动更新。Windows 二进制暂未进行
Authenticode 签名，可能触发 SmartScreen。

<!--LANG:END-->
